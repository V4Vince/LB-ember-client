{{#if isAuthenticated}}
  {{!-- Merge admin-view and visitor view components together --}}
  {{profile-page/admin-view profile=allProfiles}}
{{else}}
  {{profile-page/visitor-view profile=allProfiles }}
{{/if}}
